name: ibooking  # Your project name
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# Correct SDK constraint
environment:
  sdk: '^3.9.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  intl: ^0.19.0  # <-- Add this line if needed
  http: ^0.13.6
  google_maps_flutter: ^2.2.5
  shared_preferences: ^2.1.1

flutter:
  uses-material-design: true
  assets:
    - assets/bangunan.jpg
    - assets/bangunan1.jpg
    - assets/perkeso.png
    - assets/profile.png
  
